{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "User profile and progress tracking",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The user's unique identifier"
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address"
        },
        "displayName": {
          "type": "string",
          "description": "The user's display name"
        },
        "progress": {
          "type": "object",
          "description": "A map of topic names to the number of completed exercises"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The date and time the profile was created"
        }
      },
      "required": ["uid", "email", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Stores user profiles and their study progress."
    }
  }
}
